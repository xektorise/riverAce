class Hand:
    def __init__(self):
        self.hands = []
        
        def add_cards(self, new_cards):
            self.cards.extend(new_cards)
        
        def clear(self):
            self.cards = []
        
        def evaluate(self):
            #stärke der hand evaluieren (stright flush, flush etc.)
            pass